// This is an old script, used for drawing a dependnecy tree for only one sentence

    // svgArray.forEach(svg => {
    //   document.body.appendChild(svg);
    // });
    
    // plot each SVG object and add a line break in between
    // for (let i = 0; i < svgArray.length; i++) {
    //   document.body.appendChild(svgArray[i]);
    //   if (i < svgArray.length - 1) {
    //     const lineBreak = document.createElement('br');
    //     document.body.appendChild(lineBreak);
    //   }
    // }

    // console.log(svg);
    // append the SVG object to the body section
    // document.body.appendChild(svg);

    // sentences.forEach((sentence, index) => {
    //     const reactiveSentence = new dependencytreejs.ReactiveSentence();
    //     // console.log(reactiveSentence);
    //     reactiveSentence.fromSentenceConll(sentence);

    //     // create a circle element inside the SVG object
    //     const circle = document.createElementNS('http://www.w3.org/2000/svg', 'circle');
    //     circle.setAttribute('cx', '50');
    //     circle.setAttribute('cy', '50');
    //     circle.setAttribute('r', '30');
    //     circle.setAttribute('fill', 'red');

    //     // append the circle element to the SVG object
    //     svg.appendChild(circle);

        //
        // const targetSVGElement = document.getElementById("svgWrapper");
        // const sentenceSVGOptions = dependencytreejs.defaultSentenceSVGOptions();
        // sentenceSVGOptions.interactive = true;
        // sentenceSVGOptions.tokenSpacing = 30;
        // sentenceSVGOptions.arcHeight = 40;
        // sentenceSVGOptions.featuresHorizontalSpacing = 18;

        // const sentenceSVG = new dependencytreejs.SentenceSVG(
        // targetSVGElement,
        // reactiveSentence,
        // sentenceSVGOptions
        // );
        // console.log(targetSVGElement);
        // document.body.appendChild(targetSVGElement);
    // });


          // svgArray.push(tr);
      // svg.appendChild(tr);

    // for (var i = 0; i < svgArray.length; i++) {
    //   var svg = svgArray[i];
    //   // create a wrapper element to hold the SVG plot
    //   var wrapper = document.createElement('div');
    //   // adjust this value to set the space between svg plots
    //   wrapper.style.marginBottom = '-700px';
    //   wrapper.appendChild(svg);
    //   // append the wrapper to the container
    //   svgContainer.appendChild(wrapper);
    // }
